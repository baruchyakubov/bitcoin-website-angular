import { CoinsAmountDirective } from './coins-amount.directive';

describe('CoinsAmountDirective', () => {
  it('should create an instance', () => {
    const directive = new CoinsAmountDirective();
    expect(directive).toBeTruthy();
  });
});
